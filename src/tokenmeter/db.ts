/**
 * @deprecated — legacy path shim. Import from `src/tokenmeter/durable/legacy-path.ts` instead.
 * Only the file path under `api.state.path.state` remains, for one-time migration.
 */

export { PROJECT_DB_FILE, projectDbPath } from "./durable/legacy-path"
